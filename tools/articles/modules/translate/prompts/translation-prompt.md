Translate the following MDX content from English to {language_name}.

IMPORTANT RULES:
1. Translate ALL text content naturally and fluently to {language_name}
2. Keep ALL Markdown formatting intact (headings ##, lists -, bold **, links [], etc.)
3. Keep ALL HTML tags unchanged (like <iframe>, <div>, etc.)
4. Keep ALL URLs unchanged
5. Keep ALL YouTube video IDs unchanged
6. Maintain the same article structure and length

TRANSLATION REQUIREMENTS FOR FRONTMATTER:
- title: Must be translated to {language_name}
- description: Must be translated to {language_name}
- keywords: All keywords must be translated to {language_name}
- canonical: Update the URL to include /{lang_code}/ in the path (e.g., /en/path/ becomes /{lang_code}/path/)
- Game name: Use "{game_name}" when referring to the game
- Keep other fields (date, video, etc.) unchanged

TARGET LANGUAGE: {language_name} ({lang_code})
GAME NAME IN {language_name}: {game_name}

ORIGINAL CONTENT IN ENGLISH:

{content}

CRITICAL: Output ONLY the translated MDX content directly. DO NOT wrap it in code blocks (```mdx or ```). DO NOT add any explanations. Start directly with the frontmatter (---).

