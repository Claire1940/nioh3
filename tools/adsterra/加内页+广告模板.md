-- 社交栏广告
将其直接插入在 </body> 标签的上方。


-- 原生横幅广告  4:1


-- 以下横幅，采用 iframe 的形式包裹
-- 300x250


-- 468x60


-- 728x90


-- 160x600


(1)
阅读tools/adsterra/广告.md和横幅(非原生)广告注意事项.md、原始横幅尺寸注意事项.md和iframe广告滚动条问题修复记录.md（学习所有广告的尺寸设置经验。注意：原生横幅广告不要限制高度，让它根据实际内容大小自由显示），写一个技术文档，定义好每个页面放置位置和实现方式（不要写代码）
文章页的原生横幅，加在左侧文章和右侧相关文章的上面。原生横幅横跨整个内容区域（左侧+右侧），而不是分别在左右两侧各放一个
一个广告位在同一页面只能放置一次，每个页面至少放3个广告，但要在尽可能多的不同页面放置（尽量平衡曝光和用户体验）
一定要严格遵循横幅(非原生)广告注意事项.md！
完成文档后，等我确认。
注意事项（一定要加的）：
原生横幅广告，一定要放在文章页（包括左侧和右侧）上方、首页 hero 下方，其余页面仍可以放。
文章页的右侧边栏广告，放到相关文章和cta点击引流按钮下方。
所有文章分类导航页采用相同的配置广告，不需要区分（标题下放728x90，右侧放置160x600， 底侧放置728x90）
禁止给terms等非功能页增加广告。

(2)
遵循文档，开始重构.
改造完成后，启动服务器，使用 curl 测试所有页面，保障修改没有 bug
测试完成后，不需写测试文档，禁止关闭服务器。

（3）
请检查是否能正常构建。
执行检查代码问题：
npm run typecheck && npm run lint && vercel build
提交代码到远程仓库


-- 新增内页
（1）
分析之前内页和用户新增查询(GSC)，分析哪些关键词是没有没覆盖的，需要新增。
最后给我一个文件名为内页的Excel（含优先级都为1）。
字段分别是：
优先级、关键词（要和给你的关键词名称完全一致）、关键词搜索量、URL路径、文章标题（体现关键词且吸引人点击,必须 3个字以上，可体现出文章目标和内容。必须包含关键词）、参考链接、youtube具体视频链接，7列），必须是全英文。
表头字段命名必须是：
Priority、Keyword、Search Volume、URL Path、Article Title、Reference Link、Youtube Link

（2）
阅读tools/articles/README.md，修改tools/articles/config.json的excel_file，调用函数，为我生成新增文章